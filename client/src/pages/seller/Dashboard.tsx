
import { useAuth } from "../../context/AuthContext";

const Dashboard = () => {
  return (
    <div>
      <div>Dashboard</div>
      {/* <Logout/> */}
      {/* <button onClick={logout}>Logout</button> */}
    </div>
  );
};
export default Dashboard;